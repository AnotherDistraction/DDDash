import React from "react";
import ReactDOM from "react-dom/client";
import MarketDigestWidget from "./MarketDigestWidget";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MarketDigestWidget />
  </React.StrictMode>
);
